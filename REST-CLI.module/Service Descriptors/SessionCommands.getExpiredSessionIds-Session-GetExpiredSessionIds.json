{
  "swagger" : "2.0",
  "info" : {
    "version" : "1.0",
    "title" : "Command action is used to display a list of all MFTCC IDs that are on the session database but have expired.",
    "description" : "Command action is used to display a list of all MFTCC IDs that are on the session database but have expired."
  },
  "host" : "localhost:8083",
  "basePath" : "/",
  "schemes" : [ "http" ],
  "paths" : {
    "/session/getexpiredsessionids" : {
      "get" : {
        "description" : "Get expired session IDs",
        "operationId" : "get-session-getexpiredsessionids",
        "consumes" : [ "application/json" ],
        "produces" : [ "application/json" ],
        "parameters" : [ ],
        "responses" : {
          "200" : {
            "description" : "a string to be returned",
            "schema" : {
              "type" : "string"
            }
          }
        }
      }
    }
  },
  "definitions" : { }
}